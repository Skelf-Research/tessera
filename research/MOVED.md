# Paper artefacts have moved

The CallDNS research paper now lives in the sibling repo **`../calldns-paper`**.

This `research/` directory is retained only for history. New paper work — LaTeX,
figures, formal proofs, protocol spec, results — goes in `../calldns-paper`.

Code-side experiment harnesses (whose outputs feed the paper) live in `../calldns/scripts/`.

See the plan: `~/.claude/plans/recursive-popping-pebble.md`.
